/**
 * String manipulation:
 * refers to a set of techniques used to alter the characters in a string in order to create new strings. 
 * There are many different ways to manipulate strings in JavaScript, using both built-in functions and custom code.
 * 
 * 
 */
    concat example: 
    var x = "Hello";
    var y = "World";
    var z = x + y;
        console.log(z); // "HelloWorld"
    
    split example://takes a string and splits it to an array of the characters
    var splitWord = z.split()

    join example://takes an array and joins it to a string seperated by -
    var joinWord = splitWord.join(-)